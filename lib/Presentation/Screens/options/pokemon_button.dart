import 'package:flutter/material.dart';

class PokemonButton extends StatelessWidget {
  const PokemonButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      color: Colors.black87,
    );
  }
}
