import 'package:flutter/material.dart';

class PokemonChat extends StatelessWidget {
  const PokemonChat({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      color: Colors.red,
    );
  }
}
